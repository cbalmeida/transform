import 'package:transform/transform.dart';

class Providers extends TransformProviders {
  @override
  void registerProviders(TransformInjector injector) {
    // injector.addInstance<MyProvider>(MyProvider());
  }
}
